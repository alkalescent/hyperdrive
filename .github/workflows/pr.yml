name: Pull Request

on:
  pull_request:
    branches:
      - master

permissions:
  contents: write
  pull-requests: write
  id-token: write  # Required for OIDC trusted publishing to TestPyPI

env:
  RUN_ID: ${{ github.run_id }}
  RH_USERNAME: ${{ secrets.RH_USERNAME }}
  RH_PASSWORD: ${{ secrets.RH_PASSWORD }}
  RH_2FA: ${{ secrets.RH_2FA }}
  GLASSNODE: ${{ secrets.GLASSNODE }}
  S3_BUCKET: ${{ secrets.S3_DEV_BUCKET }}
  POLYGON: ${{ secrets.POLYGON }}
  BLS: ${{ secrets.BLS }}
  BINANCE_TESTNET_KEY: ${{ secrets.BINANCE_TESTNET_KEY }}
  BINANCE_TESTNET_SECRET: ${{ secrets.BINANCE_TESTNET_SECRET }}
  KRAKEN_KEY: ${{ secrets.KRAKEN_KEY }}
  KRAKEN_SECRET: ${{ secrets.KRAKEN_SECRET }}
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
  PREF_EXCHANGE: ${{ secrets.PREF_EXCHANGE }}
  TEST: true
  GLASSNODE_PASS: ${{ secrets.GLASSNODE_PASS }}
  ALPACA_PAPER: ${{ secrets.ALPACA_PAPER }}
  ALPACA_PAPER_SECRET: ${{ secrets.ALPACA_PAPER_SECRET }}

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yml

  qr:
    name: QR Codes
    needs: test
    runs-on: ubuntu-latest
    # Skip if triggered by QR code update commit to prevent infinite loop
    if: "!contains(github.event.head_commit.message, '[skip-qr]')"
    steps:
    - uses: actions/checkout@v5
      with:
        ref: ${{ github.head_ref }}
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    
    - name: Install uv
      uses: astral-sh/setup-uv@v7
    
    - name: Install dependencies
      run: make install
    
    - name: Generate QR codes
      run: make qr
    
    - name: Commit if changed
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add assets/
        if ! git diff --cached --quiet; then
          git commit -m "Update QR codes [skip-qr]"
          git push
        fi

  version:
    name: Version Preview
    needs: test
    runs-on: ubuntu-latest
    outputs:
      pypi_version: ${{ steps.set-version.outputs.pypi_version }}
    environment:
      name: testpypi
      url: https://test.pypi.org/p/${{ github.event.repository.name }}
    
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0  # Need full history for version calculation
    
    - name: Calculate next version (dry run)
      id: version
      uses: anothrNick/github-tag-action@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DRY_RUN: true
        DEFAULT_BUMP: minor
        WITH_V: true
        PRERELEASE: true
        PRERELEASE_SUFFIX: rc
        BRANCH_HISTORY: compare
        INITIAL_VERSION: 1.0.0
    
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    
    - name: Install uv
      uses: astral-sh/setup-uv@v7
    
    - name: Calculate PyPI version
      id: set-version
      run: |
        # Get base version from tag action (e.g., v1.2.0-rc.0)
        BASE_TAG="${NEW_TAG}"
        # Strip 'v' prefix and '-rc.X' suffix to get base version (e.g., 1.2.0)
        BASE_VERSION="${BASE_TAG#v}"
        BASE_VERSION="${BASE_VERSION%-rc.*}"
        # Create unique prerelease version: {base}rc{pr_number}.dev{run_number}
        # e.g., 1.2.0rc42.dev7 for PR #42, run #7
        PYPI_VERSION="${BASE_VERSION}rc${{ github.event.pull_request.number }}.dev${{ github.run_number }}"
        echo "Tag version: $NEW_TAG"
        echo "PyPI version: $PYPI_VERSION"
        echo "pypi_version=$PYPI_VERSION" >> $GITHUB_OUTPUT
      env:
        NEW_TAG: ${{ steps.version.outputs.new_tag }}
    
    - name: Build distributions
      run: |
        uv build --sdist
        uv build --wheel
      env:
        # Override hatch-vcs git-derived version to avoid local version identifiers (+gXXX)
        # which TestPyPI rejects
        SETUPTOOLS_SCM_PRETEND_VERSION: ${{ steps.set-version.outputs.pypi_version }}
    
    - name: Publish to TestPyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        repository-url: https://test.pypi.org/legacy/
        # Uses OIDC trusted publishing - configure at:
        # https://test.pypi.org/manage/account/publishing/
    
    - name: Comment version info on PR
      uses: actions/github-script@v8
      with:
        script: |
          const oldTag = '${{ steps.version.outputs.old_tag }}' || 'none';
          const newTag = '${{ steps.version.outputs.new_tag }}';
          const part = '${{ steps.version.outputs.part }}';
          
          // Extract base version (remove -rc.X suffix for display)
          const baseVersion = newTag.replace(/-rc\.\d+$/, '');
          
          const body = `## 📦 Version Preview
          
          - **Current version:** \`${oldTag}\`
          - **Bump type:** \`${part}\`
          - **Release version:** \`${baseVersion}\`
          - **TestPyPI version:** \`${newTag}\`
          
          When this PR is merged, version will be bumped to \`${baseVersion}\`.
          
          To change the bump type, include in commit message: \`#major\`, \`#minor\`, or \`#patch\``;
          
          // Find existing bot comment with version preview
          const { data: comments } = await github.rest.issues.listComments({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo
          });
          
          const botComment = comments.find(c => 
            c.user.type === 'Bot' && 
            c.body.includes('## 📦 Version Preview')
          );
          
          if (botComment) {
            // Check if version info changed by comparing release version and bump type
            const oldVersionMatch = botComment.body.match(/Release version:\*\* `([^`]+)`/);
            const oldBumpMatch = botComment.body.match(/Bump type:\*\* `([^`]+)`/);
            const oldReleaseVersion = oldVersionMatch ? oldVersionMatch[1] : '';
            const oldBumpType = oldBumpMatch ? oldBumpMatch[1] : '';
            
            if (oldReleaseVersion === baseVersion && oldBumpType === part) {
              console.log('Version info unchanged, skipping comment update');
              return;
            }
            
            // Update existing comment
            await github.rest.issues.updateComment({
              comment_id: botComment.id,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
            console.log('Updated existing version comment');
          } else {
            // Create new comment
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
            console.log('Created new version comment');
          }

  smoke-testpypi:
    name: Smoke TestPyPI
    needs: version
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v5
    
    - name: Wait for TestPyPI indexing
      run: |
        NAME="${{ github.event.repository.name }}"
        VERSION="${{ needs.version.outputs.pypi_version }}"
        MAX_ATTEMPTS=40
        SLEEP_SECONDS=15
        
        echo "Waiting for ${NAME}==${VERSION} to be available on TestPyPI..."
        for i in $(seq 1 $MAX_ATTEMPTS); do
          if curl -sf "https://test.pypi.org/pypi/${NAME}/${VERSION}/json" > /dev/null 2>&1; then
            echo "Package available after $((i * SLEEP_SECONDS)) seconds"
            exit 0
          fi
          echo "Attempt $i/$MAX_ATTEMPTS: Package not yet available, waiting ${SLEEP_SECONDS}s..."
          sleep $SLEEP_SECONDS
        done
        echo "Timed out waiting for package after $((MAX_ATTEMPTS * SLEEP_SECONDS)) seconds"
        exit 1
    
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    
    - name: Install from TestPyPI
      run: |
        pip install --index-url https://test.pypi.org/simple/ \
          --extra-index-url https://pypi.org/simple/ \
          ${{ github.event.repository.name }}==${{ needs.version.outputs.pypi_version }}
    
    - name: Run smoke tests
      run: python tests/smoke.py

  # =============================================================================
  # Data Pipeline Jobs (parallelized after test matrix)
  # =============================================================================

  # Wave 1: Independent data fetches (run in parallel)
  # update-symbols:
  #   name: Update Symbols
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v5
  #   - uses: actions/setup-python@v6
  #     with:
  #       python-version: '3.11'
  #   - uses: astral-sh/setup-uv@v7
  #   - run: make install
  #   - name: Update symbols
  #     run: uv run python scripts/update_symbols.py

  update-indices:
    name: Update Indices
    needs: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Update indices
      run: uv run python scripts/update_indices.py

  # update-unrate:
  #   name: Update Unemployment
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v5
  #   - uses: actions/setup-python@v6
  #     with:
  #       python-version: '3.11'
  #   - uses: astral-sh/setup-uv@v7
  #   - run: make install
  #   - name: Update unemployment
  #     run: uv run python scripts/update_unrate.py

  # # update-dividends:
  # #   name: Update Dividends
  # #   needs: test
  # #   runs-on: ubuntu-latest
  # #   steps:
  # #   - uses: actions/checkout@v5
  # #   - uses: actions/setup-python@v6
  # #     with:
  # #       python-version: '3.11'
  # #   - uses: astral-sh/setup-uv@v7
  # #   - run: make install
  # #   - name: Update dividends
  # #     run: uv run python scripts/update_dividends.py

  # # update-splits:
  # #   name: Update Splits
  # #   needs: test
  # #   runs-on: ubuntu-latest
  # #   steps:
  # #   - uses: actions/checkout@v5
  # #   - uses: actions/setup-python@v6
  # #     with:
  # #       python-version: '3.11'
  # #   - uses: astral-sh/setup-uv@v7
  # #   - run: make install
  # #   - name: Update splits
  # #     run: uv run python scripts/update_splits.py

  # Wave 2: OHLC update (depends on symbols and indices)
  update-ohlc:
    name: Update OHLC
    needs: [update-symbols, update-indices]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Update OHLC
      run: uv run python scripts/update_ohlc.py

  # # update-intraday:
  # #   name: Update Intraday
  # #   needs: [update-symbols, update-indices]
  # #   runs-on: ubuntu-latest
  # #   steps:
  # #   - uses: actions/checkout@v5
  # #   - uses: actions/setup-python@v6
  # #     with:
  # #       python-version: '3.11'
  # #   - uses: astral-sh/setup-uv@v7
  # #   - run: make install
  # #   - name: Update intraday
  # #     run: uv run python scripts/update_intraday.py

  # Wave 3: Model creation and portfolio optimization (parallel, depend on OHLC)
  create-model:
    name: Create Model
    needs: update-ohlc
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Decrypt scripts
      env:
        SALT: ${{ secrets.SALT }}
      run: uv run python util/decrypt.py encrypted/create_model.py
    - name: Create model
      run: uv run python encrypted/create_model.py

  optimize-portfolio:
    name: Optimize Portfolio
    needs: update-ohlc
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Decrypt scripts
      env:
        SALT: ${{ secrets.SALT }}
      run: uv run python util/decrypt.py encrypted/optimize_portfolio.py
    - name: Optimize portfolio
      run: uv run python encrypted/optimize_portfolio.py

  # Wave 4: Visualization and prediction (parallel, depend on model)
  visualize:
    name: Create Visualization
    needs: create-model
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Create visualization
      run: uv run python scripts/visualize.py

  predict-signal:
    name: Predict Signal
    needs: create-model
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Decrypt scripts
      env:
        SALT: ${{ secrets.SALT }}
      run: uv run python util/decrypt.py encrypted/predict_signal.py
    - name: Predict signal
      env:
        SALT: ${{ secrets.SALT }}
        EMIT_SECRET: ${{ secrets.DEV_EMIT_SECRET }}
      run: uv run python encrypted/predict_signal.py

  # Wave 5: API update and order execution (parallel, depend on signal prediction)
  update-api:
    name: Update API
    needs: predict-signal
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Update API
      env:
        SYMBOL: ${{ secrets.SYMBOL }}
      run: uv run python scripts/update_api.py

  execute-order:
    name: Execute Order
    needs: predict-signal
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - uses: astral-sh/setup-uv@v7
    - run: make install
    - name: Execute order
      run: uv run python scripts/execute_order.py
